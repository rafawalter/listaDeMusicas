import { DateTime } from "ionic-angular";

export class Evento {
    constructor(
        id: number,
        nome: string,
        datahora: DateTime,
        celebrante: string,
        temas: string[]
    ) {

    }
}